spring:
  application:
    name: ${web.name.base}
  resources:
    static-locations: file:D:/aivms5_image
  mvc:
    static-path-pattern: /**

server:
  port: ${web.port.base}
  tomcat:
    uri-encoding: ${project.encoding.charset}

