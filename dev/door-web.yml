spring:
  application:
    name: ${web.name.door}
  resources:
    static-locations: file:D:/aivms5_image
  mvc:
    static-path-pattern: /**

server:
  port: ${web.port.door}
  tomcat:
    uri-encoding: ${project.encoding.charset}