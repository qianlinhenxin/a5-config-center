spring:
  application:
    name: ${server.name.control}
  datasource:
    driver-class-name: ${database.datasource.control.control-driver-class-name}
    url: ${database.datasource.control.control-url}
    username: ${database.datasource.control.control-username}
    password: ${database.datasource.control.control-password}
  resources:
    static-locations: file:D:/aivms5_image
  mvc:
    static-path-pattern: /**

server:
  port: ${server.port.control}
  tomcat:
    uri-encoding: ${project.encoding.charset}

feign:
  hystrix:
    enabled: true

#logging:
#    level:
#        com:
#            itgo:
#                service:
#                    control:
#                        realize:
#                            mapper: TRACE


mybatis:
  mapperLocations: classpath*:/mappers/*.xml

