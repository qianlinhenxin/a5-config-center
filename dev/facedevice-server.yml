spring:
  application:
    name: ${server.name.facedevice}
  datasource:
    driver-class-name: ${database.datasource.facedevice.driver-class-name}
    url: ${database.datasource.facedevice.url}
    username: ${database.datasource.facedevice.username}
    password: ${database.datasource.facedevice.password}
  resources:
    static-locations: file:D:/aivms5_image
  mvc:
    static-path-pattern: /**

server:
  port: ${server.port.facedevice}
  tomcat:
    uri-encoding: ${project.encoding.charset}

feign:
  hystrix:
    enabled: true

#logging:
#    level:
#        com:
#            itgo:
#                service:
#                    facedevice:
#                        realize:
#                            mapper: TRACE


mybatis:
  mapperLocations: classpath*:/mappers/*.xml

