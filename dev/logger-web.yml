spring:
  application:
    name: ${web.name.logger}
  resources:
    static-locations: file:D:/aivms5_image
  mvc:
    static-path-pattern: /**
  aop:
    proxy-target-class: true
    auto: true

server:
  port: ${web.port.logger}
  tomcat:
    uri-encoding: ${project.encoding.charset}