spring:
  application:
    name: ${server.name.door}
  datasource:
    driver-class-name: ${database.datasource.door.driver-class-name}
    url: ${database.datasource.door.url}
    username: ${database.datasource.door.username}
    password: ${database.datasource.door.password}
  resources:
    static-locations: file:D:/aivms5_image
  mvc:
    static-path-pattern: /**

server:
  port: ${server.port.door}
  tomcat:
    uri-encoding: ${project.encoding.charset}

feign:
  hystrix:
    enabled: true

#logging:
#    level:
#        com:
#            itgo:
#                service:
#                    door:
#                        realize:
#                            mapper: TRACE


mybatis:
  mapperLocations: classpath*:/mappers/*.xml

