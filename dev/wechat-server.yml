spring:
  application:
    name: ${server.name.wechat}
  datasource:
    driver-class-name: ${database.datasource.wechat.wechat-driver-class-name}
    url: ${database.datasource.wechat.wechat-url}
    username: ${database.datasource.wechat.wechat-username}
    password: ${database.datasource.wechat.wechat-password}
  resources:
    static-locations: file:D:/aivms5_image
  mvc:
    static-path-pattern: /**


server:
  port: ${server.port.door}
  tomcat:
    uri-encoding: ${project.encoding.charset}

feign:
  hystrix:
    enabled: true

#logging:
#    level:
#        com:
#            itgo:
#                service:
#                    door:
#                        realize:
#                            mapper: TRACE


mybatis:
  mapperLocations: classpath*:/mappers/*.xml

