spring:
  application:
    name: ${web.name.facedevice}
  resources:
    static-locations: file:D:/aivms5_image
  mvc:
    static-path-pattern: /**

server:
  port: ${web.port.facedevice}
  tomcat:
    uri-encoding: ${project.encoding.charset}